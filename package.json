{
  "name": "edvibe-toolbox",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension utilities for Edvibe workflows",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "test": "node --require ./test-path-compat.cjs --test \"src/**/*.test.js\"",
    "test:ci": "npm test"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "2.7.1",
    "vite": "8.1.5"
  }
}
